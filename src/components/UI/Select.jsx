import Select from "react-select";

// Created to facilitate if in the future I want to use another library
// and give the posibility to keep the same API and don't break anything

export default Select;
